### My dotfiles

Apply:
```bash
$ git clone --bare https://github.com/SmallMing6675/dotfiles.git > $HOME/.dotfiles/
$ alias config='/usr/bin/git --git-dir=$HOME/.cfg/ --work-tree=$HOME'
$ config checkout
```

